# Environment Variables - Source of Truth
# 
# This file is the single source of truth for all environment variables.
# All VITE_* variables defined here will be automatically injected at runtime in Docker.
#
# For local development:
#   1. Copy this file: cp .env.example .env
#   2. Update .env with your actual values (do not commit .env)
#   3. Make sure that on adding env for development, add here too.
#
# For Docker deployment:
#   Keep values in .env and load via compose env_file.
#
# When adding new variables:
#   1. Add them here with VITE_ prefix
#   2. No code update needed; keys are auto-detected from this file

VITE_APP_NAME=GovSuite DMS
VITE_API_BASE_URL=/api

# Backend target used by Nginx reverse proxy inside the frontend container.
# This must come from .env (single source of truth).
# For host-running backend on macOS/Windows Docker Desktop:
# BACKEND_UPSTREAM=http://host.docker.internal:3000
# For backend container in same compose network:
# BACKEND_UPSTREAM=http://backend:3000
BACKEND_UPSTREAM=http://host.docker.internal:3000